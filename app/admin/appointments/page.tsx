import { createClient } from "@/lib/supabase/server";
export default async function AppointmentsPage() { const { data } = await (await createClient())!.from("appointments").select("id, patient_id, title, starts_at, status, location").order("starts_at"); return <main className="mx-auto max-w-6xl px-4 py-8"><h1 className="text-3xl font-bold">Appointments</h1><div className="mt-6 space-y-3">{(data || []).map((a: any) => <div className="rounded-lg border bg-white p-4 dark:border-slate-800 dark:bg-slate-900" key={a.id}><div className="flex justify-between gap-4"><p className="font-semibold">{a.title}</p><span className="capitalize text-sm text-teal-700">{a.status}</span></div><p className="mt-1 text-xs text-slate-500">{new Date(a.starts_at).toLocaleString()} · {a.location || "Location not set"}</p></div>)}</div></main>; }
