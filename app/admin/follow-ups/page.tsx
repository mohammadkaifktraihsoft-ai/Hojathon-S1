import { createClient } from "@/lib/supabase/server";
export default async function FollowUpsPage() { const { data } = await (await createClient())!.from("follow_up_tasks").select("id, patient_id, title, status, due_at").order("due_at"); return <main className="mx-auto max-w-6xl px-4 py-8"><h1 className="text-3xl font-bold">Follow-ups</h1><div className="mt-6 space-y-3">{(data || []).map((t: any) => <div className="rounded-lg border bg-white p-4 dark:border-slate-800 dark:bg-slate-900" key={t.id}><div className="flex justify-between gap-4"><p className="font-semibold">{t.title}</p><span className="capitalize text-sm text-teal-700">{t.status.replace("_", " ")}</span></div><p className="mt-1 text-xs text-slate-500">Patient: {t.patient_id} · Due: {t.due_at ? new Date(t.due_at).toLocaleString() : "Not set"}</p></div>)}</div></main>; }
