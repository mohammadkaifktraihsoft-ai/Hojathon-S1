import { createClient } from "@/lib/supabase/server";
import { createHospital } from "@/app/admin/actions";
export default async function HospitalsPage() { const supabase = await createClient(); const { data } = await supabase!.from("hospitals").select("id, name, location, contact_phone, updated_at").order("name"); return <main className="mx-auto max-w-5xl px-4 py-8"><h1 className="text-3xl font-bold">Hospitals</h1><p className="mt-2 text-sm text-slate-600">Participating hospitals used by Blood Availability.</p><form action={createHospital} className="mt-6 grid gap-3 rounded-lg border bg-white p-4 sm:grid-cols-4 dark:border-slate-800 dark:bg-slate-900"><input required name="name" placeholder="Hospital name" className="rounded border px-3 py-2" /><input required name="location" placeholder="Location" className="rounded border px-3 py-2" /><input name="contact_phone" placeholder="Contact (optional)" className="rounded border px-3 py-2" /><button className="rounded bg-teal-700 px-3 py-2 font-semibold text-white" type="submit">Add hospital</button></form><div className="mt-6 space-y-3">{(data || []).map((h: any) => <div className="rounded-lg border bg-white p-4 dark:border-slate-800 dark:bg-slate-900" key={h.id}><p className="font-semibold">{h.name}</p><p className="text-sm text-slate-500">{h.location} · {h.contact_phone || "No contact listed"}</p></div>)}</div></main>; }
