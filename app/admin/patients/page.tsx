import { createClient } from "@/lib/supabase/server";
export default async function PatientsPage() { const { data } = await (await createClient())!.from("patient_profiles").select("id, display_name, created_at").order("created_at", { ascending: false }); return <main className="mx-auto max-w-5xl px-4 py-8"><h1 className="text-3xl font-bold">Patients</h1><div className="mt-6 overflow-x-auto rounded-lg border bg-white dark:border-slate-800 dark:bg-slate-900"><table className="w-full text-left text-sm"><thead><tr className="border-b"><th className="p-3">Name</th><th className="p-3">Patient ID</th><th className="p-3">Created</th></tr></thead><tbody>{(data || []).map((p: any) => <tr className="border-b last:border-0" key={p.id}><td className="p-3 font-medium">{p.display_name}</td><td className="p-3 font-mono text-xs">{p.id}</td><td className="p-3">{new Date(p.created_at).toLocaleDateString()}</td></tr>)}</tbody></table></div></main>; }
