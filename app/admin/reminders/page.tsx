import { createClient } from "@/lib/supabase/server";
export default async function RemindersPage() { const { data } = await (await createClient())!.from("reminders").select("id, patient_id, task_id, remind_at, status").order("remind_at"); return <main className="mx-auto max-w-6xl px-4 py-8"><h1 className="text-3xl font-bold">Reminders</h1><div className="mt-6 space-y-3">{(data || []).map((r: any) => <div className="rounded-lg border bg-white p-4 dark:border-slate-800 dark:bg-slate-900" key={r.id}><div className="flex justify-between gap-4"><p className="font-semibold">{new Date(r.remind_at).toLocaleString()}</p><span className="capitalize text-sm text-teal-700">{r.status}</span></div><p className="mt-1 text-xs text-slate-500">Patient: {r.patient_id} {r.task_id ? `· Task: ${r.task_id}` : ""}</p></div>)}</div></main>; }
